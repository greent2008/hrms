spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hrms?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: jianengxi
    password: 123456
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: info
    com.lrm: debug
  file: log/blog-dev.log
